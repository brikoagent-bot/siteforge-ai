services:
  - type: web
    name: siteforge-ai
    runtime: node
    buildCommand: echo "No build needed"
    startCommand: node voice-agent/sms-webhook.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "3003"
